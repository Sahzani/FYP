<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>iAttend | Manage Groups</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Nunito:wght@600;700&display=swap" rel="stylesheet" />
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
  <link href="{{ url_for('static', filename='style.css') }}" rel="stylesheet">
  
  <!-- Internal Styles for Page Layout -->
  <style>
    body { margin: 0; font-family: 'Poppins', sans-serif; background: linear-gradient(75.18deg, #FFE5B4 36.63%, #F5EEDC 92.2%); }
    /* Header */
    header { position: fixed; top:0; left:0; right:0; height:60px; background:#fff; box-shadow:0 2px 20px rgba(1,41,112,0.1); display:flex; align-items:center; padding-left:20px; font-family:'Nunito',sans-serif; font-weight:700; font-size:26px; color:#212529; z-index:1000; }
    #menu-toggle { font-size:28px; margin-right:15px; cursor:pointer; }
    .header-content { width:100%; display:flex; justify-content:space-between; align-items:center; }
    .user-info { display:flex; align-items:center; gap:15px; padding:0 20px; border-radius:8px; transition:0.2s; cursor:pointer; text-decoration: none; }
    .user-info:hover { background:#f0f0f0; }
    .user-info img { width:36px; height:36px; border-radius:50%; object-fit:cover; }
    .user-info span { font-size:16px; font-weight:600; color:#444; }
    .breadcrumb { font-family:'Nunito', sans-serif; font-size:14px; font-weight:600; margin-bottom:30px; }
    .breadcrumb .section { color:#989797; }
    .breadcrumb .current { color:#444444; }

    /* Sidebar */
    .sidebar { position: fixed; top:60px; left:0; width:267px; height: calc(100% - 60px); background: linear-gradient(180deg, #FFEED3 0%, #FFEED3 100%); padding:15px 0; box-shadow:2px 0 15px rgba(1,41,112,0.08); transition: width 0.3s ease; z-index:999; border-top-right-radius:12px; overflow:hidden;}
    .sidebar.collapsed { width:70px; }
    .sidebar-section { margin-bottom:25px; padding:0 15px; }
    .sidebar-section-title { font-size:11px; text-transform:uppercase; letter-spacing:1px; color:#5D5FEF; margin-bottom:10px; padding-left:10px; transition:opacity 0.3s; }
    .sidebar.collapsed .sidebar-section-title { opacity:0; }
    .sidebar-link { display:flex; align-items:center; gap:10px; padding:12px 15px; margin-bottom:8px; border-radius:8px; font-size:15px; font-weight:500; color:#1A237E; text-decoration:none; transition:all 0.25s ease; white-space:nowrap; }
    .sidebar.collapsed .sidebar-link span { display:none; }
    .sidebar-link i { width:18px; text-align:center; }
    .sidebar-link:hover { background: rgba(255,255,255,0.5); transform: translateX(4px);}
    .sidebar-link.active { background: rgba(255,255,255,0.7); color:#4154F1; font-weight:600; }

    /* Main content */
    .main { margin-left:260px; padding:100px 30px 30px; min-height:100vh; transition:margin-left 0.3s ease;}
    .sidebar.collapsed ~ .main { margin-left:80px; }

    /* Cards */
    h2 { font-family:'Poppins',sans-serif; font-size:28px; font-weight:500; color:#444; margin-bottom:10px; }
    .grid-container { display:grid; gap:1.5rem; }
    .card-spacing { margin-bottom:1.5rem; }
    .card { background:#fff; padding:1.5rem; border-radius:12px; box-shadow:0 8px 20px rgba(0,0,0,0.08); transition: transform 0.3s ease, box-shadow 0.3s ease; position:relative; }
    .card:hover { transform:translateY(-5px); box-shadow:0 15px 30px rgba(0,0,0,0.15); }
    .stats-grid { display:grid; grid-template-columns:repeat(auto-fit, minmax(120px,1fr)); gap:1.5rem; }
    .stats-card { display:flex; flex-direction:column; align-items:center; text-align:center; }
    .icon-circle { width:3rem; height:3rem; display:flex; align-items:center; justify-content:center; background-color:#4154F1; color:white; border-radius:50%; margin-bottom:0.75rem; }

    /* Calendar */
    .calendar { display:grid; grid-template-columns:repeat(7,1fr); gap:5px; margin-top:30px; }
    .calendar-day { padding:0.5rem; border-radius:9999px; text-align:center; cursor:pointer; transition: all 0.2s ease-in-out; }
    .calendar-day.current-day { background-color:#4154F1; color:white; font-weight:700; }

    /* Notifications */
    .card p { font-size:14px; font-weight:500; }

    @media(max-width:768px){ .main { margin-left:0; padding:90px 15px; } }

    /* Page-specific styles for manage-groups */
    .manage-groups-container {
      display: flex;
      flex-wrap: wrap;
      gap: 20px;
    }
    .form-container, .groups-list-container {
      flex: 1;
      min-width: 300px;
    }
    .groups-list-container .card {
      margin-top: 0;
    }
    .group-card {
      background-color: #f9f9f9;
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 15px;
      margin-bottom: 15px;
      position: relative;
    }
    .group-card h4 {
      margin-top: 0;
      color: #333;
      border-bottom: 1px solid #eee;
      padding-bottom: 10px;
    }
    .group-card ul {
      list-style-type: none;
      padding-left: 0;
      margin-bottom: 10px;
    }
    .group-card li {
      padding: 5px 0;
      color: #555;
    }
    .group-actions {
        position: absolute;
        top: 15px;
        right: 15px;
    }
    .group-actions .btn-delete {
        background: none;
        border: none;
        color: #e74c3c;
        cursor: pointer;
        font-size: 16px;
    }
    .group-actions .btn-delete:hover {
        color: #c0392b;
    }
    #student-select {
      min-height: 150px;
    }
  </style>
</head>
<body>

<header>
  <span id="menu-toggle">&#9776;</span>
  <div class="header-content">
    <a href="{{ url_for('gc_manage_groups') }}" style="text-decoration:none;color:inherit;">iAttend</a>
    <a href="{{ url_for('teacher_profile') }}" class="user-info">
      <span>{{ profile.name }}</span>
      <img src="https://placehold.co/40x40/b8c6ff/ffffff?text=GC" alt="Group Coordinator">
    </a>
  </div>
</header>

<!-- Sidebar -->
<div class="sidebar">
  <div class="sidebar-section">
    <a class="sidebar-link" href="{{ url_for('teacher_dashboard') }}">
      <i class="fas fa-home"></i><span>Dashboard</span>
    </a>
  </div>

  <div class="sidebar-section">
    <div class="sidebar-section-title">Group Coordinator</div>
    <a class="sidebar-link active" href="{{ url_for('gc_manage_groups') }}">
      <i class="fas fa-users-cog"></i><span>Manage Groups</span>
    </a>
    <a class="sidebar-link" href="{{ url_for('gc_group_reports') }}">
      <i class="fas fa-file-alt"></i><span>Group Reports</span>
    </a>
  </div>

  <div class="sidebar-section">
    <a class="sidebar-link" href="{{ url_for('teacher_logout') }}">
      <i class="fas fa-sign-out-alt"></i><span>Logout</span>
    </a>
  </div>
</div>

<div class="main">
  <h2>Manage Groups</h2>
  <div class="breadcrumb"><span class="section">Group Coordinator</span> / <span class="current">Manage Groups</span></div>

  <!-- ADDED: Flex container for two-column layout -->
  <div class="manage-groups-container">
    
    <!-- Column 1: Form for creating/assigning -->
    <div class="form-container">
      <div class="card">
        <h3>Create a New Group</h3>
        <!-- CHANGED: Added method and action for form submission -->
        <form method="POST" action="{{ url_for('gc_manage_groups') }}">
          <label for="group-name">Group Name:</label>
          <input type="text" id="group-name" name="group_name" placeholder="e.g., 'Project Alpha'" required />

          <label for="student-select">Select Students (Hold Ctrl/Cmd to select multiple):</label>
          <!-- CHANGED: Converted to a multi-select dropdown and added name attribute -->
          <select id="student-select" name="students" multiple required>
            <!-- This part will be populated by the backend -->
            {% for student in unassigned_students %}
              <option value="{{ student.id }}">{{ student.name }}</option>
            {% endfor %}
          </select>

          <button type="submit" style="margin-top:15px;">Create Group</button>
        </form>
      </div>
    </div>

    <!-- Column 2: List of existing groups -->
    <div class="groups-list-container">
      <div class="card">
        <h3>Existing Groups</h3>
        <!-- This list will be rendered dynamically from the backend -->
        {% if groups %}
          {% for group in groups %}
          <div class="group-card">
            <h4>{{ group.name }}</h4>
            <div class="group-actions">
              <!-- ADDED: Delete form for each group -->
              <form method="POST" action="{{ url_for('gc_delete_group', group_id=group.id) }}" onsubmit="return confirm('Are you sure you want to delete this group?');">
                  <button type="submit" class="btn-delete"><i class="fas fa-trash-alt"></i></button>
              </form>
            </div>
            <ul>
              {% for member in group.members %}
                <li>{{ member.name }}</li>
              {% else %}
                <li>No students in this group yet.</li>
              {% endfor %}
            </ul>
          </div>
          {% endfor %}
        {% else %}
          <p>No groups have been created yet.</p>
        {% endif %}
      </div>
    </div>

  </div> <!-- end .manage-groups-container -->
</div>

<script>
  const menuToggle = document.getElementById('menu-toggle');
  const sidebar = document.querySelector('.sidebar');
  menuToggle.addEventListener('click', () => {
    sidebar.classList.toggle('collapsed');
  });
</script>

</body>
</html>